"""
Lab 03 Starter - Data Structure Choice

Use this file only as a starting structure.

Your job is to choose one scenario, represent it with two data structures,
compare at least three operations, and record the evidence in your README.md
file.
"""

SCENARIO_NAME = "Replace with your chosen scenario"

STRUCTURE_A_NAME = "Replace with structure A"
STRUCTURE_B_NAME = "Replace with structure B"

OPERATIONS_TO_COMPARE = [
    "add",
    "lookup",
    "update",
]

SAMPLE_RECORD = {
    "key": "TODO",
    "value": "TODO",
}

LOOKUP_KEY = "TODO"
UPDATED_VALUE = "TODO"


def create_structure_a():
    """
    Return the starting version of structure A for your scenario.

    Example idea: an empty list, set, stack, queue, or other structure that
    fits your chosen comparison.
    """
    # TODO: create structure A.
    return None


def create_structure_b():
    """
    Return the starting version of structure B for your scenario.

    Keep the information content equivalent to structure A so the comparison is
    fair.
    """
    # TODO: create structure B.
    return None


def add_record_structure_a(structure, record):
    """
    Add one record to structure A.

    Replace this docstring with a short note about how structure A handles the
    add operation.
    """
    # TODO: write the add operation for structure A.
    return None


def add_record_structure_b(structure, record):
    """
    Add one record to structure B.

    Replace this docstring with a short note about how structure B handles the
    add operation.
    """
    # TODO: write the add operation for structure B.
    return None


def lookup_record_structure_a(structure, key):
    """
    Look up one record in structure A using a key, name, ID, or similar value.
    """
    # TODO: write the lookup operation for structure A.
    return None


def lookup_record_structure_b(structure, key):
    """
    Look up one record in structure B using the same kind of key.
    """
    # TODO: write the lookup operation for structure B.
    return None


def update_record_structure_a(structure, key, updated_value):
    """
    Update one record in structure A.
    """
    # TODO: write the update operation for structure A.
    return None


def update_record_structure_b(structure, key, updated_value):
    """
    Update one record in structure B.
    """
    # TODO: write the update operation for structure B.
    return None


def main():
    print("Lab 03 starter loaded.")
    print("Next steps:")
    print("1. Choose the scenario and both structures.")
    print("2. Create equivalent starting data for both structures.")
    print("3. Implement at least three operations for each structure.")
    print("4. Compare which structure fits each operation better.")
    print("5. Record your comparison table and final recommendation in README.md.")
    print()
    print("Suggested pseudocode:")
    print("- build structure A and structure B")
    print("- add one record to each")
    print("- look up one key in each")
    print("- update one value in each")
    print("- explain which structure better fits each operation")
    print()
    print("This starter is intentionally incomplete. You must design the comparison.")


if __name__ == "__main__":
    main()
